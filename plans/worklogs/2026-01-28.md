# Worklog: 2026-01-28

---
### 10:49 - Parser Lab MCP Tools & SCM Query Infrastructure
**Author:** dinnyosz

#### Completed
- Implemented 4 new MCP tools for Magaldi self-improvement (`parser_lab_analyze`, `parser_lab_create_test`, `parser_lab_run_tests`, `parser_lab_suggest_fix`)
- Created SCM query infrastructure with `QueryRunner` class for declarative pattern matching
- Added SCM query files (elements, imports, calls) for all 4 languages: Python, JavaScript, PHP, Rust
- Converted Python extractor to use SCM queries with fallback to imperative tree-walking
- Deleted duplicate `languages/` directory (was stale code, `extractors/` was authoritative)
- Explored opportunities for new extraction features (complexity metrics, security patterns, docstring quality, etc.)

#### Decisions Made
- Chose hybrid approach: SCM queries for pattern matching, Python for post-processing
- Kept imperative fallback in Python extractor for backwards compatibility
- Simplified PHP/Rust queries after encountering grammar differences from documented patterns
- Plan saved to `plans/parser_lab_and_scm_refactor.md`

#### Commits
```
bfdd0e7 feat(extractors): add SCM queries for all languages
fc1a6e2 feat(mcp): add parser_lab tools for self-improvement
```

#### Files Touched
- 26 files changed, 2632 insertions(+), 2892 deletions(-)
- Key new files: `query_runner.py`, `tools/parser_lab.py`, `queries/{python,javascript,php,rust}/*.scm`
- Deleted: `src/magaldi_core/languages/` (5 files, ~2890 lines of stale code)

#### Notes
- All 109 parser tests pass after SCM migration
- tree-sitter query syntax differs between languages - had to inspect AST to get correct node types
- Identified high-value extraction opportunities for future: cyclomatic complexity, security patterns, type coverage, docstring quality metrics

---

---
### 11:23 - Code Metrics & Analysis Features - Planning & Setup
**Author:** dinnyosz

#### Completed
- Reviewed existing analysis infrastructure (`semantic.py`, `api_detection.py`)
- Reviewed `CodeElement` data model and ES mappings
- Analyzed MCP tool schema structure and web API patterns
- Created implementation plan for Tier 1 metrics (complexity, security, docstring quality, env vars, concurrency)
- Set up 9 tracked tasks for implementation phases

#### Decisions Made
- Will add 6 new fields to CodeElement: complexity, code_metrics, docstring_quality, security_issues, env_vars, concurrency
- Will create 3 new analysis modules: metrics.py, security.py, concurrency.py
- MCP tools will follow existing pattern in tools/schemas/ directory
- Identified that Explore agent doesn't use Magaldi MCP tools (needs skill invocation or direct use)

#### Commits
```
No new commits yet - planning phase
```

#### Files Touched
No files modified yet - implementation starting next

#### Notes
- User noted Explore agent wasn't using Magaldi tools - explained the skill invocation requirement
- Ready to begin Phase 1: Creating analysis modules

---

---
### 13:19 - Web UI Fixes and Entry Points Enhancement
**Author:** dinnyosz

#### Completed
- Fixed Elasticsearch `.keyword` suffix issues across the codebase for text field sorting and term queries
  - Browse API, repos, elements, metadata, MCP tools
  - Fields affected: `relative_path`, `name`
- Made Dashboard features and subfeatures cards clickable (navigate to Explorer filtered by type)
- Fixed `metrics_summary.security_by_severity` None value causing 500 errors
- Enhanced Entry Points page:
  - Added `http_routes` and `cli_commands` data to API response
  - Display HTTP method + path instead of raw decorators
  - Display CLI command name with framework
  - Allow multi-line summaries (removed truncation)
- Fixed imports section styling in Element detail page for dark mode compatibility

#### Decisions Made
- Used `.keyword` suffix universally for text fields to handle index mapping inconsistencies between older and newer indices
- Entry points display prioritizes extracted route/command info over raw decorators when available

#### Commits
```
d13c891 feat: improve entry points display with routes/commands, fix imports styling
3da42dd fix: handle None values in metrics_summary fields
4f5d0e8 fix: use .keyword suffix for text field term queries and sorting
f6781b7 fix: use .keyword suffix for relative_path sorting in browse API
42bf698 feat: make dashboard features and subfeatures cards clickable
```

#### Files Touched
```
src/magaldi_mcp/tools_impl.py          | 10 +++--
src/magaldi_web/frontend/src/api.ts    | 15 +++++++
src/magaldi_web/frontend/src/pages/Dashboard.tsx   | 12 +++++-
src/magaldi_web/frontend/src/pages/Element.tsx     |  8 ++--
src/magaldi_web/frontend/src/pages/EntryPoints.tsx | 46 +++++++++++++++++++++-
src/magaldi_web/models.py              | 19 +++++++++
src/magaldi_web/routes/analysis.py     | 33 +++++++++++++---
src/magaldi_web/routes/browse.py       |  4 +-
src/magaldi_web/routes/elements.py     | 20 +++++-----
src/magaldi_web/routes/repos.py        |  8 ++--
src/shared/db/repositories/elements.py |  4 +-
src/shared/db/repositories/metadata.py |  8 ++--
```

#### Notes
- The index mapping defines `relative_path` as keyword, but user's existing index has it as text (likely created before mapping change). Using `.keyword` suffix works for both cases.
- CLI command full path resolution (e.g., `magaldi serve` vs `serve`) would require tracing Click group hierarchy - deferred for now
- User's index may benefit from recreation to match current mapping schema

---
