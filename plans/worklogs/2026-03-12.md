# Worklog: 2026-03-12

---
### 10:20 - Variable Scorer v2: Training complete, catastrophic overfitting discovered
**Author:** dinnyosz

#### Completed
- Monitored v2 training to completion (Qwen2.5-1.5B-Instruct, 10 epochs, 6350 iterations, final val loss 0.047)
- Val loss progression: 0.467 → 0.109 → 0.084 → 0.068 → 0.056 → 0.047
- Fused LoRA adapters into merged model (3.09 GB safetensors)
- Converted merged model to GGUF f16 format via convert_hf_to_gguf.py (homebrew llama.cpp)
- Quantized to both Q8_0 (1565 MiB) and Q4_K_M (935 MiB)
- Registered both with Ollama as `magaldi-scorer` and `magaldi-scorer-q4km`
- Created Q4_K_M Modelfile at `tools/training/exports/Modelfile.q4km`
- Updated main Modelfile to point to v2 Q8_0 GGUF
- Ran evaluation script against both models — discovered catastrophic overfitting
- Model produces garbage output ("andy \nInI the variable value value value...") despite low val loss
- This confirms the Deep Research section's prediction that 10 epochs is too many

#### Decisions Made
- Exported both Q8_0 and Q4_K_M quantizations for comparison (per user's research recommendations)
- Updated Modelfile to point to v2 GGUF path

#### Recent Commits
```
c91b798 fix: update ollama model names to distinguish by model size
02c81ec feat: auto-detect training configs and prompt user to choose
f88b80a feat: add benchmark, scoring comparison, and 0.5b training config
8482475 docs: add plans, decision logs, and worklogs from recent sessions
04e62ab feat: add llama.cpp setup, update training pipeline paths and evaluator
6090f95 docs: add TODOs for scoring dimensions expansion and subagent issues
27af8a9 refactor: revert model defaults from vllm-mlx to ollama
b5822cf refactor: rename --resume to --cache and default sample size to 10k
c02c11d feat: add train-variable-scorer orchestrator skill
fe499ed feat: add training data rebalancing to undersample trivial examples
```

#### Files Touched
- `tools/training/exports/Modelfile` - Updated FROM path to v2 Q8_0 GGUF
- `tools/training/exports/Modelfile.q4km` - New: Q4_K_M variant Modelfile
- `tools/training/exports/magaldi-variable-scorer-qwen2.5-1.5b-v2-f16.gguf` - New: f16 GGUF
- `tools/training/exports/magaldi-variable-scorer-qwen2.5-1.5b-v2-q8_0.gguf` - New: Q8_0 GGUF
- `tools/training/exports/magaldi-variable-scorer-qwen2.5-1.5b-v2-q4_k_m.gguf` - New: Q4_K_M GGUF
- `tools/training/models/variable-scorer-qwen2.5-v2/merged/` - New: fused model
- `tools/training/models/variable-scorer-qwen2.5-v2/adapters/` - 63 checkpoint files (every 100 iters)

#### Open Questions
- Should we try earlier checkpoints (epoch 2-3 adapters at iters 1300-1900) before investing in a full v3 training run?
- The user's v3 config recommends many changes at once (3 epochs, LR 2e-4, rank 8, mask_prompt: false, cosine decay) — should these be tested incrementally or all at once?
- Q4_K_M vs Q8_0: both produce garbage currently, but once a working model is found, which quantization to use for production?

#### Notes
- Val loss kept decreasing throughout all 10 epochs (0.467 → 0.047) but this was misleading — it measured token prediction accuracy on the val set, not generalization
- The 63 saved checkpoints (every 100 iters) are valuable — they allow testing earlier epochs without retraining
- Epoch 2 ≈ iter 1270, Epoch 3 ≈ iter 1905 — these are the most promising checkpoints to try
- mlx-lm fuse doesn't support --adapter-file flag for selecting specific checkpoints; need to copy the checkpoint file to adapters.safetensors and re-fuse
- Peak training memory was 9.1 GB throughout
- Training speed: 0.15-0.18 it/sec, total ~12 hours for 6350 iterations

---
### 12:36 - Training config cleanup and skill auto-detection
**Author:** dinnyosz

#### Completed
- Committed pending uncommitted changes in logical groups: model defaults revert (vllm-mlx → ollama), CLAUDE.md TODOs, training pipeline files (gitignore, SKILL.md, llama.cpp README, evaluate_scorer)
- Removed scip investigation directory and wordcloud artifacts
- Committed plans, decision logs, worklogs from previous sessions, plus benchmark/comparison tool scripts and 0.5b training config
- Added auto-detection of training configs to train-variable-scorer skill: scans `tools/training/configs/variable_scorer*.yaml`, extracts key details, presents options via AskUserQuestion
- Updated Ollama model names to be size-specific: `magaldi-scorer-1.7b` and `magaldi-scorer-0.5b`
- Aligned 0.5b training config hyperparameters with 1.7b (same rank 8, alpha 16, dropout 0.05, 3 epochs, cosine decay LR) for fair comparison
- User adjusted 0.5b to batch_size=2/gradient_accumulation_steps=4 (same effective batch of 8, lower per-step memory)

#### Decisions Made
- Config auto-detection: scan glob pattern `variable_scorer*.yaml`, present via AskUserQuestion, skip prompt if only one config exists
- Aligned 0.5b hyperparameters with 1.7b for fair apples-to-apples comparison — only `base_model` and `ollama_model_name` differ
- Size-specific Ollama model names (`magaldi-scorer-1.7b` vs `magaldi-scorer-0.5b`) to avoid conflicts when both are registered

#### Recent Commits
```
84a07ef docs: update variable scorer training doc with v2 results
bf028c1 feat: add llama-cpp and openai backends to evaluate_scorer
1bb5ffe fix: align 0.5b training config with 1.7b hyperparameters
141bec5 feat: skip repo parsing when raw data already sufficient
c91b798 fix: update ollama model names to distinguish by model size
02c81ec feat: auto-detect training configs and prompt user to choose
f88b80a feat: add benchmark, scoring comparison, and 0.5b training config
8482475 docs: add plans, decision logs, and worklogs from recent sessions
```

#### Files Touched
```
 .claude/skills/train-variable-scorer/SKILL.md    |  78 +-
 plans/variable_scorer_training.md                | 133 +++-
 src/magaldi_core/variable_scoring/__init__.py    |  55 +-
 src/magaldi_core/variable_scoring/models.py      |  23 +-
 src/magaldi_core/variable_scoring/prompts.py     |  32 +-
 src/shared/cli/_runners.py                       |   6 +
 tests/test_training_pipeline.py                  | 164 +++++
 tests/variable_scoring/test_scoring.py           | 123 ++--
 tools/benchmark_vllm_metal.py                    | 841 +++++++++++++++++++++++
 tools/compare_scoring_models.py                  | 247 +++++++
 tools/training/configs/variable_scorer.yaml      |   4 +-
 tools/training/configs/variable_scorer_0.5b.yaml |  33 +
 tools/training/evaluate_scorer.py                | 278 +++++++-
 tools/training/generate_scoring_data.py          | 390 +++++++----
 14 files changed, 2109 insertions(+), 298 deletions(-)
```

#### Notes
- The 0.5b config differs from 1.7b only in batch_size (2 vs 4) and gradient_accumulation_steps (4 vs 2) — same effective batch size of 8, but smaller memory footprint for the 0.5B model
- `fused_model/` directory (1.1GB merged safetensors) remains untracked — not committed per user request
- Additional uncommitted changes exist in variable_scoring module and configs from work done between context-summary calls

---
### 13:37 - Variable scorer JSON structured output and conservative tokenization
**Author:** dinnyosz

#### Completed
- Implemented JSON structured output format for variable scorer to replace regex-based parsing
- Updated `src/magaldi_core/variable_scoring/__init__.py`: modified scoring prompt to request JSON format with all 7 dimensions (importance, redundancy, complexity, value_complexity, naming_quality, scope_significance, semantic_richness)
- Updated `src/shared/ai/llm_client.py`: added json_mode parameter to LiteLLM calls, implemented `_parse_json_response()` method with validation
- Added comprehensive test suite in `tests/test_llm_client.py` covering JSON parsing, fallback to regex for non-JSON-mode models, error handling
- Updated `tests/variable_scoring/test_scoring.py`: modified score assertion logic to match new JSON output structure
- Changed token estimation ratio in `tools/training/generate_scoring_data.py` from 4 chars/token to 3 chars/token for more conservative code tokenization
- Updated `tools/training/configs/variable_scorer.yaml`: removed num_layers parameter (was 28), reduced batch_size from 4 to 2, increased gradient_accumulation_steps from 2 to 4 for lower per-step memory usage

#### Decisions Made
- JSON structured output provides more robust parsing than regex and enables validation of all 7 dimensions
- More conservative 3 chars/token ratio for code blocks since code tokenizes less efficiently than prose
- Training config memory optimization: batch_size reduction paired with gradient accumulation increase maintains same effective batch size (8) while reducing per-step memory

#### Recent Commits
```
c8d0e3b feat: switch variable scoring to JSON structured output
3b9d5d4 fix: use conservative 3 chars/token ratio in training data packing
3a9b9c2 docs: add training session decision logs and worklog
a713682 chore: clean up training configs and gitignore fused_model
679fa73 feat: add v2 scorer model config and production prompt eval flag
dc0f582 feat: expand variable scoring from 4 to 7 dimensions
84a07ef docs: update variable scorer training doc with v2 results
bf028c1 feat: add llama-cpp and openai backends to evaluate_scorer
1bb5ffe fix: align 0.5b training config with 1.7b hyperparameters
141bec5 feat: skip repo parsing when raw data already sufficient
```

#### Files Touched
```
 src/magaldi_core/variable_scoring/__init__.py |  16 +-
 src/shared/ai/llm_client.py                   |  36 ++++-
 tests/test_llm_client.py                      | 206 ++++++++++++++++++++++++++
 tests/variable_scoring/test_scoring.py        |  24 +++
 tools/training/configs/variable_scorer.yaml   |   4 +-
 5 files changed, 274 insertions(+), 12 deletions(-)
```

#### Open Questions
- JSON mode support varies by LLM backend (OpenAI GPT-4 supports it, Ollama models may not) — fallback logic handles this but haven't tested against all backends yet
- Need to verify that next variable scorer training run achieves comparable accuracy with the lower batch_size and more conservative tokenization ratio

#### Notes
- JSON structured output eliminates manual regex parsing and makes dimension validation explicit
- The LiteLLM json_mode parameter is silently ignored by backends that don't support it, so fallback logic gracefully degrades to regex parsing
- Token estimation is critical for training data packing since we target 3B tokens per epoch — 3 chars/token is more realistic for code than the original 4 chars/token

---
