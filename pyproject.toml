[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "magaldi"
version = "0.1.0"
description = "Code discovery engine for AI agents and developers"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.11"
authors = [
    {name = "Magaldi Project"}
]
keywords = ["code", "search", "semantic", "ai", "mcp", "claude"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Text Processing :: Indexing",
]
dependencies = [
    # CLI
    "click>=8.1.0",
    # Environment
    "python-dotenv>=1.0.0",
    "rich>=13.0.0",
    # Config
    "pyyaml>=6.0",
    # HTTP
    "requests>=2.31.0",
    # Database
    "mysql-connector-python>=8.2.0",
    "elasticsearch>=8.11.0,<9.0.0",
    "redis>=5.0.0",
    # Web
    "fastapi>=0.104.0",
    "uvicorn[standard]>=0.24.0",
    # Parser
    "tree-sitter>=0.21.0",
    # MCP
    "mcp>=1.0.0",
    # ML/Visualization
    "numpy>=1.24.0",
    "scikit-learn>=1.3.0",
    "umap-learn>=0.5.4",
    # Testing
    "pytest>=7.4.0",
    "pytest-cov>=4.1.0",
    "pytest-asyncio>=0.21.0",
    # Linting & Type checking
    "ruff>=0.1.0",
    "mypy>=1.7.0",
    "types-PyYAML>=6.0.0",
    "types-requests>=2.31.0",
]

[project.scripts]
magaldi = "magaldi.cli:main"

[project.urls]
Homepage = "https://github.com/magaldi/magaldi"
Documentation = "https://github.com/magaldi/magaldi#readme"
Repository = "https://github.com/magaldi/magaldi"
Issues = "https://github.com/magaldi/magaldi/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
magaldi = ["py.typed"]

# =============================================================================
# RUFF (linting + formatting)
# =============================================================================

[tool.ruff]
target-version = "py311"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E",      # pycodestyle errors
    "W",      # pycodestyle warnings
    "F",      # pyflakes
    "I",      # isort
    "B",      # flake8-bugbear
    "C4",     # flake8-comprehensions
    "UP",     # pyupgrade
    "ARG",    # flake8-unused-arguments
    "SIM",    # flake8-simplify
]
ignore = [
    "E501",   # line too long (handled by formatter)
    "B008",   # do not perform function calls in argument defaults
    "B905",   # zip without strict= (Python 3.10+)
]

[tool.ruff.lint.isort]
known-first-party = ["magaldi"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

# =============================================================================
# MYPY (type checking)
# =============================================================================

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
strict_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
show_error_codes = true
namespace_packages = true
explicit_package_bases = true

[[tool.mypy.overrides]]
module = [
    "tree_sitter.*",
    "elasticsearch.*",
    "redis.*",
    "umap.*",
    "sklearn.*",
]
ignore_missing_imports = true

# =============================================================================
# PYTEST
# =============================================================================

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = [
    "-v",
    "--strict-markers",
    "--tb=short",
]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests that require external services",
]
asyncio_mode = "auto"

# =============================================================================
# COVERAGE
# =============================================================================

[tool.coverage.run]
source = ["src/magaldi"]
branch = true
omit = [
    "*/tests/*",
    "*/__init__.py",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise NotImplementedError",
    "if TYPE_CHECKING:",
    "if __name__ == .__main__.:",
]
